prepare:
  image_extensions: [".jpg", ".jpeg", ".png"]

split:
  seed: 42

train:
  epochs: 30
  batch_size: 32
  lr: 0.001
  model_arch: resnet18
  strategy: "linear_probe"
  early_stopping_patience: 5

evaluate:
  batch_size: 32
