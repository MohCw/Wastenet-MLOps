prepare:
  image_extensions: [".jpg", ".jpeg", ".png"]

split:
  seed: 42

train:
  epochs: 30
  batch_size: 32
  lr: 0.001
  model_arch: resnet18.tv_in1k
  strategy: "linear_probe" # linear_probe | full_finetune | partial_finetune
  early_stopping_patience: 5
  scheduler: "none"  # none | cosine | plateau
  warmup_epochs: 3  

evaluate:
  batch_size: 32
